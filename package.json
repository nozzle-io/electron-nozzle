{
  "name": "electron-nozzle",
  "version": "0.1.0",
  "private": true,
  "description": "Electron/nozzle texture integration feasibility spike with honest GPU blockers and CPU-copy sample paths.",
  "main": "app/main.js",
  "type": "commonjs",
  "scripts": {
    "test": "node --test test/*.test.js",
    "smoke:electron": "node scripts/run-electron-smoke.js",
    "check:package": "node scripts/check-package-shape.js"
  },
  "engines": {
    "node": "22.13.1"
  },
  "devDependencies": {
    "electron": "42.4.0"
  }
}
